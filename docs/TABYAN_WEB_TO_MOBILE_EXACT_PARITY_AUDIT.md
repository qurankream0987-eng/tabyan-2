# TABYAN WEB → MOBILE EXACT PARITY AUDIT

**التاريخ:** 2026-09-07 (Asia/Riyadh)
**المصدر المرجعي الوحيد:** `artifacts/tabyan` (الموقع)
**الاتجاه:** WEB → MOBILE فقط — لم يُعدّل الموقع إطلاقًا
**الهوية المقفلة:** Bundle `app.replit.tbyan`، Expo Project `bcac43ba-905a-4b13-a834-b36051da4da6`، Version `1.0.0`، iOS Build `6` — بدون أي تغيير

## PHASE 1 — Freeze

- Commit مرجعي: `8b6cb6baaf1dd129ffb7b24270033b194e90ccd7` (حالة ما قبل مرحلة التطابق هذه؛ التغييرات الجديدة غير مُرسلة بعد).
- حجم `artifacts/mobile` قبل التعديل: **1.5M** | حجم `artifacts/mobile/assets`: **388K** | حجم `artifacts/tabyan`: 543M.
- عدد شاشات Mobile (route files): 76.
- ⚠️ **نسخة QCF الاحتياطية `/home/runner/tabyan-qcf-detached-build6` غير موجودة في هذه الجلسة.** QCF مفصول من البناء فعليًا (0 ملف في export)، لكن موقع النسخة الاحتياطية الموثّق سابقًا لم يُعثر عليه الآن. لم يُحذف أي شيء في هذه المرحلة.

## PHASE 2 — موقع الويب (ملخص التدقيق)

- **ضيف:** صفحة ترحيب تسويقية، أزرار دخول/إنشاء حساب، بدون OTP ("لا يوجد رمز تحقق").
- **طالب:** الرئيسية (تحية + آية اليوم + تقدم + مسارات + مواعيد)، القرآن (`QuranMenu`: حفظ ومراجعة القرآن / تصحيح التلاوة / القراءات مع شارة إجازة)، المستويات (`Levels.tsx`: الغرس/السنبلة/الزرع/الثمرة/الوارثون + تجويد + شرعية مع شروط ومتطلبات ونصوص ثابتة)، اختبار القبول، الحجز، الجدول، التسجيلات، المكتبة، الفتاوى، الإشعارات، الحساب/الإعدادات.
- **معلم:** لوحة، جدول، طلاب، تقييمات، مراجعة اختبارات، KYC، فتاوى، رسائل جماعية، إعدادات.
- **مشرف:** KPIs، مستخدمون، مراجعة طلاب/معلمين، جداول، مستويات، علوم شرعية، قراءات، ترقيات، مكتبة، فتاوى/مفتون، تقييمات، تحليلات، إشعارات، سجل تدقيق، إعدادات.
- **التصميم:** burgundy `#800020` / gold `#D4AF37`، خط الواجهة IBM Plex Sans Arabic، الخط القرآني Amiri؛ أيقونات SVG مخصصة في `SectionIcon.tsx`.

## PHASE 3 — Mobile (الفجوات المؤكدة قبل الإصلاح)

| WEB_SCREEN | MOBILE_SCREEN (قبل) | STATUS | EXACT_GAP |
|---|---|---|---|
| `/student/quran` QuranMenu | `student/quran.tsx` | WRONG | كان يعرض «المصحف/الحفظ والمراجعة/التلاوة» بدل «حفظ ومراجعة القرآن/تصحيح التلاوة/القراءات»، وبلا شارة الإجازة |
| `/student/levels/:pathId` Levels | — | MISSING | لا توجد شاشة مستويات Native إطلاقًا |
| Tab المصحف | `(tabs)/mushaf-fahd` | MOBILE_ONLY (خارج النطاق) | QCF معطل — تبويب يقود لقارئ غير متوفر |
| مكتبة: `/student/library/[id]` | يُوجَّه إلى `/student/library/book/:id` | WRONG | مسار مكسور |
| SVG SectionIcon مخصص | Ionicons عامة | PARTIAL | أيقونات Web المرسومة غير منقولة بعد |
| بقية الشاشات (جدول/مكتبة/فتاوى/حجز/اختبار قبول/معلم/مشرف) | موجودة | PARTIAL | موجودة وظيفيًا؛ لم تُفحص بصريًا pixel-by-pixel بعد |

## الإصلاحات المنفذة في هذه المرحلة

1. **إزالة المصحف من تنقل Mobile**: حذف تبويب «المصحف» من الشريط السفلي، وإبقاء المسار مخفيًا (`href: null`) بدل حذف المصدر — لا رابط ميت ولا شاشة «غير متوفر» في التنقل.
2. **شاشة القرآن Native = Web QuranMenu**: «حفظ ومراجعة القرآن» → `/student/levels/quran`، «تصحيح التلاوة» → `/student/tilawah`، «القراءات» → `/student/ijazat` مع شارة «إجازتك معتمدة / قيد المراجعة / يتطلب إجازة» من `student.paths`.
3. **شاشة مستويات Native جديدة** `app/student/levels/[pathId].tsx`: نفس أسماء المستويات، شروط الالتحاق، متطلبات الحلقة، متطلب العقيدة الإلزامي (الثمرة 1–137، الوارثون 137–290)، دروس التجويد، أسماء العرض (المستوى الأول/الثاني/الثالث)، بطاقات الشرعية (عقيدة/فقه/سيرة)، زر «دخول اختبار القبول» / «اختر مقعداً — جدول الشيوخ»، ورابط «اختر حلقة في هذا المسار».
4. **إصلاح رابط الكتاب المكسور** في تبويب المكتبة.

## الملفات المعدّلة

- `artifacts/mobile/app/student/(tabs)/_layout.tsx` (إزالة تبويب المصحف)
- `artifacts/mobile/app/student/quran.tsx` (إعادة بناء كاملة لتطابق QuranMenu)
- `artifacts/mobile/app/student/levels/[pathId].tsx` (جديد)
- `artifacts/mobile/app/student/(tabs)/library.tsx` (إصلاح مسار)

## البوابات التقنية

- MOBILE_TYPECHECK: **PASS**
- IOS_METRO_EXPORT: **PASS** (2302 modules، حجم export 13M)
- QCF_FONTS_IN_IOS_EXPORT: **0**
- QCF_PAGE_JSON_IN_IOS_EXPORT: **0**
- MUSHAF_ASSETS_INCLUDED: **NO**
- GIT_DIFF_CHECK: **PASS**
- الهوية (Bundle/Project/Version/Build): **بدون تغيير**

## ما لم يُستكمل (موثّق بصراحة)

- الفحص البصري جنبًا إلى جنب (Web vs Mobile) للشاشات الأخرى: لم يتم بعد.
- SVG المخصصة من `SectionIcon.tsx` لم تُنقل إلى Native (الموبايل يستخدم Ionicons) — PARTIAL.
- تجربة المعلم والمشرف موجودة لكن لم تُطابق تفصيليًا — PARTIAL.
- شهادة الشرعية (`/student/sharia/certificate/:levelId`) وبعض مسارات الشرعية العميقة لا تزال غير مطابقة الشكل.
- نسخة QCF الاحتياطية الموثقة سابقًا غير موجودة في الجلسة الحالية — يجب التحقق/إعادة إنشائها قبل أي إصدار نهائي يعيد المصحف.
