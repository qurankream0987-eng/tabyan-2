import { useEffect, useState } from "react"; import * as Location from "expo-location"; import { StudentScreen, LoadingState, ErrorState, Card } from "./_screen"; import { Text } from "react-native"; import { useTheme } from "../../lib/theme"; import { userFacingErrorMessage } from "../../lib/user-facing-error";
export default function Qibla(){const [angle,setAngle]=useState<number>();const [error,setError]=useState("");const {colors}=useTheme();useEffect(()=>{(async()=>{const p=await Location.requestForegroundPermissionsAsync();if(!p.granted)throw Error("لم يتم منح إذن الموقع");const l=await Location.getCurrentPositionAsync({});const lat=l.coords.latitude*Math.PI/180,lon=l.coords.longitude*Math.PI/180,qLat=21.4225*Math.PI/180,qLon=39.8262*Math.PI/180;const a=Math.atan2(Math.sin(qLon-lon),Math.cos(lat)*Math.tan(qLat)-Math.sin(lat)*Math.cos(qLon-lon))*180/Math.PI;setAngle((a+360)%360);})().catch(e=>setError(userFacingErrorMessage(e,"تعذر تحديد اتجاه القبلة.")));},[]);if(error)return <StudentScreen title="القبلة"><ErrorState message={error}/></StudentScreen>;if(angle===undefined)return <StudentScreen title="القبلة"><LoadingState label="نحدد اتجاه القبلة من موقعك…" /></StudentScreen>;return <StudentScreen title="القبلة"><Card><Text style={{color:colors.text,textAlign:"center",fontSize:42}}>{Math.round(angle)}°</Text><Text style={{color:colors.muted,textAlign:"center"}}>زاوية القبلة من الشمال — البوصلة غير متاحة، حرّك الجهاز يدوياً.</Text></Card></StudentScreen>}